List to debug:

1. Fixing the reset button so that the puzzle will retract any labels that are on the puzzle board

2. Multiple labels should not be dropping into the zone. Make it so only one label can be dropped at a time.

3. Creating a highlight area when hovering label over a droppable area in the human anatomy puzzle. The draggable label will also change colour when being hovered over area zone. Make it so that both highlights will be removed when no longer hovering.

Steps to fix for each bug:

PROBLEM 1:
- To make everything reset, need to target what needs to be resetted, which are the labels (not the drop zones!!). Then we need to associate it with the reset button (#reset-btn?) and make it go back to its original location.
- in js, need to target all labels (so target the class) for reset so make it a vairable. Also make the reset button a variable so it can be targetted.
- to activate the function itself, use event listener to watch out if the reset button gets clicked.
- in functios section, make it a function that all the labels that are in the target zones will reset



PROBLEM 2:
- To make the labels only drop in a zone one at a time, should tell js to not let the label drop in the droppable area to be placed if there is an existing element (the CHILD??) in da zone.
- the current file has dropped function, so fix it in the function dropped area. (fixed in class) Currently it is already fixed to prevent the double drops, where it recognizes if the piece is being dragged by using an if statment. If the child (label) has 1 label in it already, it drops the piece my appending it (the child) and then reset it
- add console log so it's more clear that the zone is full, for clarity sake

-(Personal side NOTES, Googled what is currentDraggedElement = null)
     - means you are intentionally setting a variable named currentDraggedElement to have no value or to be empty. 
     - serving the following purposes:
     - Intentional Empty State: It declares that no element is currently being dragged.
     - Resetting/Cleaning Up: It clears out a previously stored reference to a DOM element, usually after a drag operation has finished (dragend) or when no drag is happening.
     - Preventing Errors: It explicitly sets the state, ensuring that the code does not accidentally try to interact with a previously dragged, non-existent element.



3. PROBLEM 3:
- Highlight the area when hovering over with the label! Must need to make js recognize when dragging and hovering over the area will mean something, which would mean targeting the drop zone and labels as well.
- Make an event listener to see the trigger of dragging over, then direct it to the function
- make a function when being dragged over the target zone, under the function dragOver.
- since it's changing the colour, make a new class for just hovering in css (so it can be changed to that when the function calls for it.)
- When exiting, the colour has to be removed.
- Needs to add 2 new functions: enter (the zone) and exit while dragging.